{
    "experiment": {
        "question": "${data.question}"
    },
    "evaluation": {
        "math_evaluation_flow": {
            "question": "${data.question}",
            "context": "${data.context}",
            "answer": "${run.outputs.answer}"
        }
    }

}
